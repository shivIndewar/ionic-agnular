import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'its.shivs.test',
  appName: 'ionic-agnular',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
